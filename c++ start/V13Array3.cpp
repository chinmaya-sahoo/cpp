// find the last occurance(index) of an element x in a given array (try using vector)
#include <iostream>
#include <vector>
using namespace std;
int main()
{
    vector<int>v(6);
    for (int i = 0; i < 6; i++)
    {
        cin>>v[i];
    }
    
    for (int i = 0; i < 6; i++)
    {
        cout<<v[i]<<" ";
    }
    
}